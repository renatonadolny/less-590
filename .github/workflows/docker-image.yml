name: Docker Image CI

on:
  push:
    branches: [ review ]
  pull_request:
    branches: [ review ]

jobs:

  build:

    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build -t my-gcc-app --tag my-image-name:$(date +%s)
      
      
